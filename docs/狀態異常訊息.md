# 狀態異常訊息 (etlStatus = "狀態異常") 觸發條件一覽

> 本文件為 ETL「狀態異常」之 `etlReason` 訊息與程式碼錨點的 SSOT（文件入口：[docs/README.md](./README.md)）。
> 錨點行號最後校訂：2026-09-24。

在 UI 「工作表表單編碼提取結果」中，當狀態被判定為**「狀態異常」**（即 `etlStatus === '狀態異常'`）時，**「原因說明」**（`etlReason`）欄位所呈現的異常細節與其觸發原因可歸納為以下三類：

### 1. 系統/檔案讀取層面錯誤
此類錯誤發生在檔案無法正常載入或進行基礎的 Excel 解析時：
* **`讀取錯誤 ({錯誤原因})`** (例如：`讀取錯誤 (檔案結構損毀)`)
  * **觸發原因**：Vite 讀取檔案失敗、檔案結構毀損，或 SheetJS 解析 Workbook 時發生錯誤。
  * **程式碼位置**：[App.jsx:L400](../src/App.jsx#L400)
* **`開啟失敗 ({錯誤原因})`** (例如：`開啟失敗 (未知錯誤)`)
  * **觸發原因**：檔案存取時發生未預期的系統層級 JavaScript 異常。
  * **程式碼位置**：[App.jsx:L413](../src/App.jsx#L413)

---

### 2. QIP 專屬檢驗 (射出/押出) 資料夾月份判定錯誤
此類錯誤主要發生在 QIP (QC10004-R02/QIP押出) 製程檢驗中，當程式試圖從其父資料夾提取月份進行 ETL 歸類時，路徑或月份不符合規則：
* **`QIP射出：資料夾名稱未包含月份字尾 (無法判定月份)`** 
  * **觸發原因**：射出檔案所在的父資料夾名稱結尾未包含月份格式（如 `-03`）。
  * **程式碼位置**：[excelParser.js:L102](../src/utils/excelParser.js#L102)
* **`QIP射出：資料夾月份 [{月份}] 不合法`**
  * **觸發原因**：射出檔案父資料夾結尾提取出的月份數值不合法（非 1 ~ 12 月）。
  * **程式碼位置**：[excelParser.js:L107](../src/utils/excelParser.js#L107)
* **`QIP射出：無效的路徑結構`**
  * **觸發原因**：檔案的相對路徑結構層級不足（無法以 `/` 切割出父資料夾）。
  * **程式碼位置**：[excelParser.js:L134](../src/utils/excelParser.js#L134)
* **`QIP押出：資料夾名稱未包含月份字尾 (無法判定月份)`**
  * **觸發原因**：同射出，押出檔案的父資料夾名稱結尾未包含月份格式。
  * **程式碼位置**：[excelParser.js:L144](../src/utils/excelParser.js#L144)
* **`QIP押出：資料夾月份 [{月份}] 不合法`**
  * **觸發原因**：同射出，押出檔案的父資料夾月份數值非 1 ~ 12 月。
  * **程式碼位置**：[excelParser.js:L149](../src/utils/excelParser.js#L149)
* **`QIP押出：無效的路徑結構`**
  * **觸發原因**：同射出，押出檔案的相對路徑結構層級不足。
  * **程式碼位置**：[excelParser.js:L183](../src/utils/excelParser.js#L183)

---

### 3. 一般品檢表關鍵欄位缺失
此類錯誤發生在一般品檢（如完成品、半成品等）檔案解析成功，但對照 ETL 核心三要素（QC表單編碼、品檢子分類、月份）時有缺失：
* **`一般品檢：欄位缺失 (QC編碼: {編碼}, 子分類: {子分類}, 月份: {月份})`**
  * **觸發原因**：只要解析出來的 `QC編碼`、`子分類` (如裝配A/Tubing等) 或 `月份` 之中有任何一項為空 (顯示為 `無`)，或是月份數值不合法（非 1 ~ 12），便會拋出此異常。
  * **常見細節範例**：
    * `一般品檢：欄位缺失 (QC編碼: QC10007-R03, 子分類: 裝配A, 月份: 無)` —— 代表工作表無法解析出月份。
    * `一般品檢：欄位缺失 (QC編碼: 無, 子分類: 無, 月份: 5)` —— 代表工作表不是可識別的 QC 表單或無法解析出子分類。
  * **程式碼位置**：[excelParser.js:L344](../src/utils/excelParser.js#L344)